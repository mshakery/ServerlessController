syntax = "proto3";

package kubernetes;

option go_package="protos/protos";

message NodeName {
  string name = 1;
}

message Empty {}

service MetricCollector {
  rpc GatherMetric(NodeName) returns (Empty) {}
}
